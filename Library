Must Learn (High Priority)

1. os, sys, subprocess   → System automation
2. requests              → API interactions
3. boto3                 → Cloud (AWS)
4. pyyaml                → Config files
5. paramiko              → SSH automation
6. docker                → Container management
7. pytest                → Testing your scripts

Good to Know (Medium Priority)

8. kubernetes       → manage K8s resources
9. logging         → proper log management
10. argparse        → command line tools
11. re              → regex for log parsing

Bonus (Nice to Have)   -- Ignore now

12. schedule        → run scripts on schedule
13. smtplib         → send email alerts
14. slack_sdk       → send Slack alerts
15. prometheus_client→ expose metrics
16. fabric          → remote server automation
-----------------------------------------------------------------------------------------------------
Module
→ a single Python file

Library
→ collection of modules/packages

------------------------------------------------------------------------
Python Module            Purpose                     Bash Alternative
-----------------------------------------------------------------------------
1. os, sys, subprocess   System automation           shell commands

2. requests              REST API calls                   curl / wget

3. boto3                 AWS cloud automation             AWS CLI

4. json                  JSON parsing                     jq

5. pyyaml                YAML config handling             yq / grep / sed

6. paramiko              SSH automation                   ssh / scp / rsync

7. docker                Docker container management      docker CLI

8. kubernetes            Kubernetes automation            kubectl

9. logging              Log management                   echo / logger

10. argparse             Command line arguments           getopts

11. re                   Regex & log parsing              grep / sed / awk

----------------------------------------------------------------------------

# File system
ls -la
export ENV=prod

# Run commands
bash deploy.sh

# API call
curl https://api.example.com

# AWS
aws s3 ls

# JSON parsing
jq '.name' file.json

# YAML parsing
yq '.name' config.yaml

# SSH
ssh user@server

# Copy files
scp app.jar user@server:/opt/apps/

# Docker
docker ps

# Kubernetes
kubectl get pods

# Logging
logger "Deployment started"

# CLI arguments
./script.sh -e prod

# Regex / log parsing
grep "ERROR" app.log
awk '{print $1}' file.txt

# Bash testing
bats test.sh
-------------------------------------------------------
Summary:

Real DevOps usage

Most DevOps engineers use BOTH:

Bash
- Jenkins pipelines
- Shell scripts
- Linux admin
- kubectl/docker commands
- Quick fixes

Python
- AWS automation
- Complex deployments
- Monitoring tools
- API automation
- Kubernetes automation
- Custom DevOps platforms
---------------------------------------------------------------------
Industry preference

| Scenario                 | Preferred |
| ------------------------ | --------- |
| Simple server automation | Bash      |
| CI/CD scripts            | Bash      |
| Cloud automation         | Python    |
| Kubernetes automation    | Python    |
| Large scalable tools     | Python    |
| One-liner commands       | Bash      |

---------------------------------------------------------------------------
For Kubernetes + DevOps career:

- Learn Bash first
- Then Python automation
- Then Kubernetes SDKs & boto3

That combination is highly valuable in DevOps/SRE roles.

